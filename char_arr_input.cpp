#include <bits/stdc++.h>
using namespace std;
int main()
{
    // char word[10];
    // cin>>word;  // Ignore white space
    // cout<< "Your word char = " << word << endl;
    // cout<< strlen(word);


    char sentence[30];
    cin.getline(sentence, 30);
    cout<< "Your Sentence arr = " << sentence << endl;
    cout<< strlen(sentence);
    

    // char sentence_terminate[30];
    // cin.getline(sentence_terminate, 30, '*');
    // cout<< "Your sentence terminate at the point of * = " << sentence_terminate << endl;
    // cout<< strlen(sentence_terminate);
}